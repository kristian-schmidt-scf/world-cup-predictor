# No API key required for the default data sources.
# football-data.org key is optional — used only if you swap fetchMatches.js
# to use their paid tier instead of the free GitHub CSV source.
# FOOTBALL_DATA_API_KEY=your_key_here
